server:
  port: 8081

spring:
  application:
    name: iam-service
  datasource:
    url: jdbc:mysql://localhost:3306/biotrack_iam
    username: root
    password: Sania@2611
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
