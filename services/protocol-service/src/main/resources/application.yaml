server:
  port: 8086

spring:
  application:
    name: protocol-service
  datasource:
    url: jdbc:mysql://localhost:3306/biotrack_protocol
    username: root
    password: Sania@2611
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true

logging:
  level:
    org.springframework.security: DEBUG
    root: INFO
